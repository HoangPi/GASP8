#pragma once

#include <limits>

namespace MaxValue
{
    const float MaxFloat = std::numeric_limits<float>::max();
    const double MaxDouble = std::numeric_limits<double>::max();
    const int TraceTarget = 4;
}
